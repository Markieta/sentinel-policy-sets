import "tfconfig"

using_tags = func() {
  for tfconfig.modules as name, module {
    if module.source not contains "ref=" {
      return false
    }
  }

  return true
}

main = rule {
  using_tags()
}